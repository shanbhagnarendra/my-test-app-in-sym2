{% extends '::base.html.twig' %}

{% block body -%}
    <h1>HrmState</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Statename</th>
                <td>{{ entity.stateName }}</td>
            </tr>
            <tr>
                <th>Statecode</th>
                <td>{{ entity.stateCode }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('hrmstate') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('hrmstate_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('hrmstate_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
