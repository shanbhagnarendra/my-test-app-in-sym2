{% extends '::base.html.twig' %}

{% block body -%}
    <h1>HrmCountry list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Countryname</th>
                <th>Countrycode</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('hrmcountry_show', { 'id': entity.id }) }}">{{ entity.countryName }}</a></td>
                <td>{{ entity.countryCode }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('hrmcountry_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('hrmcountry_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('hrmcountry_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
