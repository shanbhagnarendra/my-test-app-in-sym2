{% extends '::base.html.twig' %}

{% block body -%}
    <h1>HrmCountry</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Countryname</th>
                <td>{{ entity.countryName }}</td>
            </tr>
            <tr>
                <th>Countrycode</th>
                <td>{{ entity.countryCode }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('hrmcountry') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('hrmcountry_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('hrmcountry_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
